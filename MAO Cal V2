<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>MAO Quick Calculator</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    * {
      box-sizing: border-box;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    body {
      margin: 0;
      padding: 0;
      background: #0f172a;
      color: #e5e7eb;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .container {
      background: #020617;
      border-radius: 22px;
      padding: 24px 24px 30px;
      max-width: 520px;
      width: 100%;
      box-shadow: 0 24px 60px rgba(0,0,0,0.7);
      border: 1px solid #1f2937;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 24px;
    }

    .title {
      font-size: 1.1rem;
      font-weight: 600;
      letter-spacing: 0.12em;
      text-transform: uppercase;
      color: #e5e7eb;
    }

    .formula {
      font-size: 0.85rem;
      color: #9ca3af;
    }

    .formula span {
      font-weight: 500;
      color: #e5e7eb;
    }

    .section {
      margin-bottom: 24px;
    }

    .section-label {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 4px;
      font-size: 0.92rem;
      font-weight: 600;
    }

    .section-helper {
      font-size: 0.76rem;
      color: #9ca3af;
      margin-bottom: 10px;
    }

    .value-with-input {
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .current-value {
      font-size: 0.95rem;
      font-weight: 500;
      color: #f9fafb;
    }

    .pill-input {
      border-radius: 999px;
      border: 1px solid #374151;
      background: #020617;
      padding: 4px 12px;
      font-size: 0.8rem;
      display: inline-flex;
      align-items: center;
      gap: 4px;
      color: #e5e7eb;
      min-width: 90px;
      justify-content: flex-end;
    }

    .pill-input input {
      border: none;
      background: transparent;
      color: #e5e7eb;
      text-align: right;
      width: 60px;
      font-size: 0.8rem;
    }

    .pill-input input:focus {
      outline: none;
    }

    input[type="range"] {
      width: 100%;
      -webkit-appearance: none;
      appearance: none;
      height: 6px;
      border-radius: 999px;
      background: #111827;
      outline: none;
      margin: 10px 0 4px;
    }

    input[type="range"]::-webkit-slider-thumb {
      -webkit-appearance: none;
      appearance: none;
      width: 18px;
      height: 18px;
      border-radius: 50%;
      background: #22c55e;
      cursor: pointer;
      border: 2px solid #bbf7d0;
      box-shadow: 0 0 0 4px rgba(34,197,94,0.25);
    }

    input[type="range"]::-moz-range-thumb {
      width: 18px;
      height: 18px;
      border-radius: 50%;
      background: #22c55e;
      cursor: pointer;
      border: 2px solid #bbf7d0;
      box-shadow: 0 0 0 4px rgba(34,197,94,0.25);
    }

    .scale-labels {
      display: flex;
      justify-content: space-between;
      margin-top: 6px;
      font-size: 0.74rem;
      color: #9ca3af;
    }

    .scale-labels span {
      width: 25%;
      text-align: center;
      border-top: 1px dashed #1f2937;
      padding-top: 4px;
    }

    .mao-card {
      margin-top: 12px;
      padding: 16px 18px 20px;
      border-radius: 18px;
      background: radial-gradient(circle at top left, #22c55e33, transparent 55%), #020617;
      border: 1px solid #16a34a;
      text-align: center;
    }

    .mao-label {
      font-size: 0.85rem;
      letter-spacing: 0.16em;
      text-transform: uppercase;
      color: #9ca3af;
      margin-bottom: 6px;
    }

    .mao-value {
      font-size: 2.6rem;
      font-weight: 700;
      color: #bbf7d0;
      line-height: 1.1;
    }

    .mao-subtext {
      margin-top: 6px;
      font-size: 0.8rem;
      color: #9ca3af;
    }

    .summary {
      margin-top: 18px;
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
      font-size: 0.8rem;
      color: #9ca3af;
    }

    .summary-item {
      flex: 1 1 45%;
    }

    .summary-label {
      text-transform: uppercase;
      letter-spacing: 0.12em;
      font-size: 0.7rem;
      color: #6b7280;
      margin-bottom: 4px;
    }

    .summary-value {
      font-size: 0.9rem;
      font-weight: 500;
      color: #e5e7eb;
    }

    .crm-section {
      margin-top: 18px;
      font-size: 0.8rem;
    }

    .crm-header-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 2px;
    }

    .crm-title {
      font-weight: 600;
    }

    .crm-subtitle {
      font-size: 0.75rem;
      color: #9ca3af;
      margin-bottom: 8px;
    }

    .copy-btn {
      border-radius: 999px;
      border: 1px solid #22c55e;
      background: #022c22;
      color: #bbf7d0;
      font-size: 0.75rem;
      padding: 6px 18px;
      cursor: pointer;
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    .copy-btn:hover {
      background: #065f46;
    }

    .crm-box {
      margin-top: 6px;
      border-radius: 18px;
      border: 1px solid #1f2937;
      background: #020617;
      padding: 10px 14px;
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
      font-size: 0.8rem;
      line-height: 1.5;
      color: #e5e7eb;
      white-space: pre-line;
    }

    @media (max-width: 540px) {
      .container {
        margin: 16px;
      }
      .mao-value {
        font-size: 2.2rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="title">MAO QUICK CALCULATOR</div>
      <div class="formula"><span>MAO</span> = 70% of ARV − Repairs</div>
    </div>

    <!-- ARV -->
    <div class="section">
      <div class="section-label">
        <span>After Repair Value (ARV)</span>
        <div class="value-with-input">
          <span id="arv-display" class="current-value">$285,000</span>
          <div class="pill-input">
            <input id="arv-input" type="number" min="150" max="450" step="5" value="285" />
            <span>k</span>
          </div>
        </div>
      </div>
      <div class="section-helper">
        Slide or type ARV in thousands (e.g. <strong>285</strong> = $285,000). 150–450 recommended.
      </div>
      <input id="arv-slider" type="range" min="150000" max="450000" step="5000" value="285000" />
      <div class="scale-labels">
        <span>$150k</span><span>$250k</span><span>$350k</span><span>$450k</span>
      </div>
    </div>

    <!-- Rehab -->
    <div class="section">
      <div class="section-label">
        <span>Rehab Budget</span>
        <div class="value-with-input">
          <span id="rehab-display" class="current-value">Medium · $45,000</span>
          <div class="pill-input">
            <input id="rehab-input" type="number" step="1000" value="45000" />
            <span>$</span>
          </div>
        </div>
      </div>
      <div class="section-helper">
        Slide for Light / Medium / Large / Gut, or type your exact rehab budget.
      </div>
      <input id="rehab-slider" type="range" min="0" max="3" step="1" value="1" />
      <div class="scale-labels">
        <span>Light<br/>$25k</span>
        <span>Medium<br/>$45k</span>
        <span>Large<br/>$65k</span>
        <span>Gut<br/>$85k</span>
      </div>
    </div>

    <!-- MAO -->
    <div class="mao-card">
      <div class="mao-label">MAX ALLOWABLE OFFER</div>
      <div id="mao-display" class="mao-value">$154,500</div>
      <div class="mao-subtext">Based on 70% of ARV minus repairs.</div>
    </div>

    <!-- Summary -->
    <div class="summary">
      <div class="summary-item">
        <div class="summary-label">ARV</div>
        <div id="summary-arv" class="summary-value">$285,000</div>
      </div>
      <div class="summary-item">
        <div class="summary-label">Repairs</div>
        <div id="summary-rehab" class="summary-value">$45,000 (Medium)</div>
      </div>
      <div class="summary-item">
        <div class="summary-label">70% of ARV</div>
        <div id="summary-70arv" class="summary-value">$199,500</div>
      </div>
      <div class="summary-item">
        <div class="summary-label">Formula</div>
        <div id="summary-formula" class="summary-value">199,500 − 45,000 = 154,500</div>
      </div>
    </div>

    <!-- CRM NOTE -->
    <div class="crm-section">
      <div class="crm-header-row">
        <div class="crm-title">CRM Note (copy & paste)</div>
        <button id="copy-btn" class="copy-btn">COPY</button>
      </div>
      <div class="crm-subtitle">Ready to copy into your CRM notes.</div>
      <div id="crm-box" class="crm-box"></div>
    </div>
  </div>

  <script>
    const arvSlider = document.getElementById('arv-slider');
    const arvInput  = document.getElementById('arv-input');
    const arvDisplay = document.getElementById('arv-display');

    const rehabSlider = document.getElementById('rehab-slider');
    const rehabInput  = document.getElementById('rehab-input');
    const rehabDisplay = document.getElementById('rehab-display');

    const maoDisplay = document.getElementById('mao-display');

    const summaryArv = document.getElementById('summary-arv');
    const summaryRehab = document.getElementById('summary-rehab');
    const summary70arv = document.getElementById('summary-70arv');
    const summaryFormula = document.getElementById('summary-formula');

    const crmBox = document.getElementById('crm-box');
    const copyBtn = document.getElementById('copy-btn');

    const rehabOptions = [
      { label: 'Light', amount: 25000 },
      { label: 'Medium', amount: 45000 },
      { label: 'Large', amount: 65000 },
      { label: 'Gut', amount: 85000 }
    ];

    function formatCurrency(num) {
      return num.toLocaleString('en-US', {
        style: 'currency',
        currency: 'USD',
        maximumFractionDigits: 0
      });
    }

    function recalc() {
      const arv = parseInt(arvSlider.value, 10) || 0;
      const rehab = parseInt(rehabInput.value, 10) || 0;

      const seventy = Math.round(arv * 0.7);
      const mao = Math.max(0, seventy - rehab);

      // ARV + MAO display
      arvDisplay.textContent = formatCurrency(arv);
      maoDisplay.textContent = formatCurrency(mao);

      // Rehab label
      const match = rehabOptions.find(o => o.amount === rehab);
      const rehabLabel = match ? match.label : 'Custom';
      rehabDisplay.textContent = `${rehabLabel} · ${formatCurrency(rehab)}`;

      // Summary
      summaryArv.textContent = formatCurrency(arv);
      summaryRehab.textContent = `${formatCurrency(rehab)} (${rehabLabel})`;
      summary70arv.textContent = formatCurrency(seventy);
      summaryFormula.textContent =
        `${seventy.toLocaleString('en-US')} − ${rehab.toLocaleString('en-US')} = ${mao.toLocaleString('en-US')}`;

      // CRM note text
      crmBox.textContent =
        `ARV: ${formatCurrency(arv)}\n` +
        `Assuming Rehab: ${formatCurrency(rehab)}\n` +
        `MAO offer: ${formatCurrency(mao)} (make offer)`;
    }

    function syncARVFromSlider() {
      const arv = parseInt(arvSlider.value, 10) || 0;
      arvInput.value = Math.round(arv / 1000);
      recalc();
    }

    function syncARVFromInput() {
      let k = parseInt(arvInput.value, 10);
      if (isNaN(k)) k = 150;
      if (k < 150) k = 150;
      if (k > 450) k = 450;
      arvSlider.value = k * 1000;
      recalc();
    }

    function syncRehabFromSlider() {
      const idx = parseInt(rehabSlider.value, 10) || 0;
      const opt = rehabOptions[idx];
      rehabInput.value = opt.amount;
      recalc();
    }

    function syncRehabFromInput() {
      let v = parseInt(rehabInput.value, 10);
      if (isNaN(v) || v < 0) v = 0;
      rehabInput.value = v;
      recalc();
    }

    arvSlider.addEventListener('input', syncARVFromSlider);
    arvInput.addEventListener('change', syncARVFromInput);

    rehabSlider.addEventListener('input', syncRehabFromSlider);
    rehabInput.addEventListener('change', syncRehabFromInput);

    copyBtn.addEventListener('click', async () => {
      try {
        await navigator.clipboard.writeText(crmBox.textContent);
        copyBtn.textContent = 'COPIED';
        setTimeout(() => copyBtn.textContent = 'COPY', 1500);
      } catch {
        alert('Press Cmd + C (or Ctrl + C) to copy.');
      }
    });

    // Initialize on load
    syncARVFromSlider();
    syncRehabFromSlider();
  </script>
</body>
</html>
